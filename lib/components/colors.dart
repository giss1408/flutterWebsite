import 'package:flutter/painting.dart';

const Color primary = Color(0xFF1389FD);
const Color primaryDark = Color(0xFF0276E8);
const Color primaryLight = Color(0xFFEFF8FF);

/// Near-black — strong heading + body contrast (WCAG AAA on white).
const Color textPrimary = Color(0xFF1E293B);
/// Muted slate for subtitles, captions, secondary copy.
const Color textSecondary = Color(0xFF64748B);

const Color buttonPrimaryDark = Color(0xFF0075E6);
const Color buttonPrimaryDarkPressed = Color(0xFF006ED9);
const Color buttonPrimaryPressedOutline = Color(0xFF93C5FD);

/// Page background — near-white with a barely perceptible cool tint.
const Color background = Color(0xFFF9FAFB);
/// Deep slate-900 for footer / dark CTA sections.
const Color backgroundDark = Color(0xFF0F172A);
/// Solid subtle divider — consistent on any white/light surface.
const Color border = Color(0xFFE5E7EB);
