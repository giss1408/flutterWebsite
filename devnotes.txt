# docker build -t <DOCKER NAME> .
# docker run -d -p <PORT FOR DOCKER>:<THE EXPOSE PORT> <DOCKER NAME>
# docker run -it --rm -p <PORT FOR DOCKER>:<THE EXPOSE PORT> <DOCKER NAME>

### List all images
docker ps -a
docker ps -f
docker rm
docker images -f
docker rmi
Docker v1.9.0 release notes
docker volume ls
docker volume rm


# From chatgpt
* docker build -t flutter-web-app .
* docker run -it --rm -p 8080:8080 flutter-web-app



docker stop $(docker ps -q) && docker rm $(docker ps -aq --filter ancestor=flutter-web-app)
docker build --no-cache -t flutter-web-app .
docker run -d -p 8080:80 flutter-web-app



# Local test
flutter build web --release
cd build/web && python3 -m http.server 8080
